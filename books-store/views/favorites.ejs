<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Favorite Books</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      body {
        font-family: "Poppins", sans-serif;
      }
    </style>
  </head>
  
  <body class="bg-gray-100">
    <div class="max-w-4xl mx-auto p-6">
      <h1 class="text-3xl font-bold text-center mb-6">My Favorite Books</h1>

      <% if (books.length > 0) { %>
        <% for (book of books) { %>
          <% if (book.isFavourite) { %>
            <div class="bg-white p-6 rounded-lg shadow-lg hover:shadow-xl transition-shadow duration-300 mb-4">
              <h2 class="text-xl font-semibold text-gray-800 mb-2">
                Title: <%= book.title %>
              </h2>
              <p class="text-gray-700 mb-2">Authors: <%= book.authors.join(', ') %></p>
              <p class="text-gray-700 mb-2">Description: <%= book.description %></p>
              <p class="text-gray-700 mb-2">
                <a href="<%= book.moreInfoUrl %>" class="text-blue-500 hover:underline" target="_blank">More Info</a>
              </p>
            </div>
          <% } %>
        <% } %>
      <% } else { %>
        <div class="text-center">
          <p class="text-lg text-gray-500">No favorite books found.</p>
        </div>
      <% } %>
    </div>
  </body>
</html>
